import { Routes } from '@angular/router';
// Since the original app uses signal-based view switching, a traditional
// routing module is not strictly necessary but is included here for completeness.

export const routes: Routes = [];